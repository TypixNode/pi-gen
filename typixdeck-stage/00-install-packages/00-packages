# Overlay compilation (01-overlays builds the .dtbo files in the chroot)
device-tree-compiler

# MCU flashing tools for /opt/typixdeck/firmware (04-firmware-tools):
# esptool for the ESP32-S3 co-processor, dfu-util for the KeebDeck STM32
esptool
dfu-util

# On-device kernel module / native development. Headers for the dwc2fix
# kernel come from its own deb (00-install-kernel); these cover the stock
# fallback kernels (v8 = CM4/Pi4, 2712 = CM5/Pi5).
build-essential
linux-headers-rpi-v8
linux-headers-rpi-2712

# Basic editor
vim

# venv for esptool >= 5 (11-provision); console fonts + chvt/setfont for its tty1 progress
python3-venv
kbd
console-setup
